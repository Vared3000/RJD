# Протокол приёмки Windows-сервера

Этот протокол заполняется после установки на целевом сервере. Он отделяет
автоматически проверяемые факты от проверок, для которых нужны перезагрузка,
рабочие места заказчика и внешний сетевой контур.

## Автоматический отчёт

1. Выполните `Создать резервную копию.bat`.
2. Проверьте восстановление именно свежей копии:

   ```powershell
   powershell.exe -NoProfile -ExecutionPolicy Bypass `
     -File .\scripts\verify-backup.ps1 -LatestDaily -TestRestore
   ```

3. Запустите задания `Workwear ERP Daily Backup` и
   `Workwear ERP Monthly Restore Test` вручную и дождитесь результата `0x0`.
4. Запустите `Провести приёмку.bat` от имени администратора.
5. Приложите созданные файлы `logs/acceptance/acceptance-*.md` и `.json` к
   этому протоколу.

Сценарий ничего не изменяет. Он проверяет версии Windows, Node.js, pnpm и
PostgreSQL, автозапуск службы, локальный и LAN healthcheck, правило firewall,
закрытые служебные порты, отсутствие Cloudflare Tunnel, результаты заданий,
SHA-256 основной и второй резервных копий. Ненулевой код означает, что хотя бы
одна автоматическая проверка не пройдена.

## Ручная проверка

| Пункт | Дата и результат | Проверяющий |
|---|---|---|
| После полной перезагрузки служба доступна без входа пользователя |  |  |
| Вход и открытие карточки работника с рабочего места №1 |  |  |
| Вход и формирование печатной формы с рабочего места №2 |  |  |
| Из другой подсети TCP 80 недоступен |  |  |
| Из интернета у сервера нет публичного адреса/переадресации порта |  |  |
| PostgreSQL 5432 и backend 4000 недоступны с рабочих мест |  |  |
| Последняя копия восстановлена во временную БД, количества совпали |  |  |
| Из временной БД сформирована и визуально проверена печатная форма |  |  |

Сервер/IP: ____________________  Подсеть: ____________________

Commit релиза: ____________________  Дата приёмки: ____________________

Итог: принято / принято с замечаниями / не принято.

Представитель заказчика: ____________________ / ____________________

Представитель исполнителя: ____________________ / ____________________
