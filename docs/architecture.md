# Архитектурные соглашения

Эти правила действуют для всех последующих этапов (см. PROJECT_SPECIFICATION.md,
раздел 17) и не должны нарушаться без явного пересмотра — цель проекта прямо
требует масштабируемости без переписывания ядра.

## Backend: Controller → Service → Repository

Каждый модуль в `server/src/modules/<module>/`:

- `*.routes.js` — только маршруты + JSDoc для Swagger, вызывает контроллер.
- `*.controller.js` — HTTP-слой: парсинг запроса, вызов сервиса, формирование
  ответа через `utils/respond.js`. Никакой бизнес-логики.
- `*.service.js` — бизнес-логика и оркестрация. Не знает про `req`/`res`.
- `*.repository.js` — единственное место обращения к Sequelize-моделям.
- `*.validation.js` — zod-схемы входных данных.

Ошибки — только через `ApiError` (`utils/api-error.js`) или естественные
исключения Sequelize/zod; центральный `errorHandler` сам сопоставит их с HTTP-
статусами. Не отправлять `res.status().json()` с ошибкой напрямую из сервиса/
репозитория.

## Данные

- Первичные ключи — `UUID` (`DataTypes.UUIDV4`) везде, без исключений.
- Столбцы в БД — `snake_case` (`underscored: true` в sequelize.js), в JS-коде —
  `camelCase` через `field:` в определении модели.
- Мягкое удаление — через `archivedAt` (nullable `DATE`), без физического
  DELETE и без отдельного булева `isActive`/`status` для этой цели. Причина:
  складские документы и печатные формы должны сохранять корректные ссылки на
  архивные записи (номенклатура, работники, склады и т.д.) даже после архивации.
  Выборки для **создания новых** документов фильтруют `archivedAt: null`;
  `include` в **уже существующих** документах — без фильтра.
- Изменение остатков склада — только через складские документы (раздел 11 ТЗ),
  никогда прямым UPDATE количества.

## Права доступа

Единый каталог прав — `server/src/config/permissions.js` (коды вида
`module.action`, например `nomenclature.manage`). Роли — `RolePermission`
многие-ко-многим. При добавлении нового модуля: добавить его права в каталог,
привязать к нужным ролям, защищать роуты через
`requirePermission('module.action')`.

## Frontend: Feature-Sliced Design

Слои и правило зависимостей (выше может импортировать только из нижних слоёв):

```
app → pages → widgets → features → shared
```

- `shared/` не импортирует ничего из `features/pages/widgets` — в частности,
  состояние сессии живёт в `shared/session/session-store.js`, а не в
  `features/auth`, потому что `shared/api/http-client.js` должно иметь к нему
  доступ для авто-обновления токена по 401.
- `entities/` пока не создан — появится, когда возникнет первая настоящая
  бизнес-сущность с собственной моделью данных на фронтенде (работник,
  номенклатура и т.п.), а не раньше (без пустых слоёв "про запас").
- Токен доступа хранится только в памяти (Zustand), не в localStorage —
  устойчивость к XSS. При перезагрузке страницы сессия молча восстанавливается
  через `POST /auth/refresh` (httpOnly cookie).
- Стили — CSS Modules, колокейтед с компонентом (`Component.module.css`).
  Общие токены (цвета, отступы) — CSS-переменные в `app/styles/global.css`.

## CatalogPage / EntityFormModal: конфигурируемые список+форма

`features/catalogs/ui/CatalogPage.jsx` — общий компонент (таблица +
модальная форма + архивирование/восстановление), которым реализованы все
справочники и номенклатура (см. `pages/catalogs/*`, `pages/nomenclature/*`).
Сама форма-в-модалке вынесена в `features/catalogs/ui/EntityFormModal.jsx` —
набор полей (см. `CatalogFormField.jsx`) + zod-схема + `defaultValues`;
`CatalogPage` — один из потребителей, наравне со страницами документов
(`pages/purchases/Receiving*Page.jsx`), которым нужна форма-в-модалке, но не
подходит вся остальная механика `CatalogPage` (архивация, простой список).
При использовании `EntityFormModal` в новом месте:

- **Если в zod-схеме поля есть `.default(...)`, продублируйте его в
  `field.defaultValue`.** RHF инициализирует пустые поля пустой строкой, а не
  `undefined`, поэтому `.default()` в схеме молча не сработает и форма
  покажет ошибку валидации на пустом с виду поле. `withSafeDefaults()` внутри
  `EntityFormModal` подставляет `field.defaultValue`, только если само поле
  отсутствует в переданных `defaultValues` (создание — `{}`/`undefined` по
  полю; правка — берётся значение из редактируемой записи).
- **Числовые поля регистрируются без `valueAsNumber`** (см.
  `CatalogFormField.jsx`) — иначе RHF превращает пустую строку в `NaN` ещё до
  zod, и `z.preprocess` для опциональных числовых полей (пример:
  `instance.validation.js`, поле `cost`) не успевает увидеть пустую строку.
  Строка → число приводит сама схема через `z.coerce.number()`.
- `optionValue`/`optionLabel` в конфиге `select`-поля принимают либо строку
  (имя свойства), либо функцию `(item) => ...` — используйте функцию, когда
  подпись нужно собрать из нескольких полей (пример: размер в
  `InstancesPage.jsx` — `"Обувь: 42"`).
- Если у сущности есть собственное бизнес-поле с названием "статус" (как
  `Instance.status` — этап жизненного цикла), передавайте в `CatalogPage`
  `archiveColumnLabel`, чтобы не путать его со встроенной колонкой
  архивации/восстановления.
- `Modal` рендерит `role="dialog"` — если на странице одновременно видны две
  кнопки с одинаковым текстом (например, "Провести" и в шапке, и в модалке
  подтверждения), различать их по этому атрибуту, а не только по тексту.

## Документы (Поступление и далее): шапка + строки + проведение

Первый пример — `server/src/modules/purchases/receiving/`. Следующие
складские документы (Выдача, Возврат, Перемещение, Инвентаризация, Списание —
Этапы 8/10) устроены концептуально так же; обобщать в фабрику пока рано (см.
`reference-crud.factory.js` — тот был оправдан 6 одинаковыми сущностями сразу,
здесь пока одна) — переоценить после 2-3 документов.

- Статусы только `draft`/`posted` — черновик редактируем (шапка и строки),
  проведённый — нет (проверяется в сервисе, не полагаться на UI).
- **Проведение — одна транзакция** (`sequelize.transaction`): создаёт
  побочные эффекты (партия, экземпляры, движения склада) и переводит
  документ в `posted` атомарно.
- **Блокировка на проведении — только шапка документа**
  (`SELECT ... FOR UPDATE` без `include`), не строки одним запросом с
  джойном: Postgres не разрешает `FOR UPDATE` через LEFT JOIN, а именно так
  Sequelize строит `include` для `hasMany`, когда у документа может не быть
  ни одной строки. Строки — отдельным запросом в той же транзакции.
- Массовая генерация номеров (`generateInventoryNumbers(count)` в
  `nomenclature/instances/generate-inventory-number.js`) — одним запросом
  через `generate_series`, а не N последовательных `nextval()` в цикле.
- `StockMovement` (`server/src/database/models/stock-movement.model.js`) —
  общая инфраструктура движений склада, `document_id` намеренно без FK (тип
  документа в `document_type` определяет, в какой таблице искать) — её
  заполняет каждый складской документ при проведении, а не отдельный модуль.

## Тесты, создающие данные в dev-БД

`server/src/tests/*.test.js` ходят в реальную dev-базу (не в транзакции,
которая откатывается) — **каждый тест обязан подчистить за собой** через
`t.after(async () => { ... })` с прямым удалением через `models.X.destroy()`
(не через API — архивация мягким удалением не убирает строку). Порядок
удаления должен уважать FK (`RESTRICT` почти везде): сначала
`StockMovement`/`Instance`, потом `ReceivingDocument` (строки каскадом),
потом `Batch`, потом склады/поставщики/номенклатура/размеры, и только в
конце — `Organization`. Если тест использует `Date.now()` для уникальности
имён — использовать один и тот же `unique` во всех связанных сущностях
и в cleanup, а не выдумывать отдельную строку для удаления (несуществующее
имя в `DELETE ... WHERE name = ...` тихо удалит ноль строк и оставит мусор).

## Именование модулей (рус. ТЗ → англ. код)

| Раздел ТЗ | Код |
|---|---|
| Работники | employees |
| ДПО | dpo |
| Организации/Подразделения/Должности/Склады/Поставщики/Размеры | catalogs (справочники) |
| Номенклатура | nomenclature |
| Закупки/Оприходование | purchases |
| Выдача/Возврат | issuance |
| Перемещение | transfers |
| Инвентаризация | inventory |
| Стирка | laundry |
| Ремонт | repair |
| Списание | writeoff |
| Отчёты | reports |
| Печатные формы | print_forms |
| Администрирование | admin |

Придерживаться этих названий в путях API, таблицах БД и правах — чтобы не
плодить синонимы (`warehouse` vs `stock` и т.п.) между этапами.
