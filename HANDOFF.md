# HANDOFF: текущий статус и следующая задача

> Открой этот файл первым делом в начале сессии (подгружается автоматически
> через `CLAUDE.md`). В отличие от одноразовых HANDOFF-файлов, этот —
> постоянный: обновляй секцию "Текущий статус" и "Следующая задача" по мере
> продвижения, не удаляй файл.

## Автономность

Раздел 16 `PROJECT_SPECIFICATION.md` уже даёт разрешение не спрашивать
подтверждения для безопасных команд, npm install/uninstall, создания
файлов/папок/компонентов, миграций, моделей, роутов, сервисов, рефакторинга,
удаления собственных временных файлов. Исключение — предупреждать перед
потенциально разрушительными действиями (массовое удаление кода, изменение
схемы БД без миграции, удаление данных, изменение истории Git). Это уже
подтверждено на практике за 5 этапов работы — просто продолжай в том же духе,
не переспрашивая "начать с X?".

## Текущий статус (обновлено на конец сессии, реализовавшей Этап 6)

Закоммичено ранее (`git log --oneline`):

```
33ed252 Добавить CLAUDE.md и HANDOFF.md для передачи контекста между сессиями
5bd1f73 Этап 5: закупки и оприходование (документ Поступление)
d17d8e8 Этап 4: номенклатура (Модель → Размер → Экземпляр)
8a63353 Этап 3: справочники (организации, подразделения, должности, склады, поставщики, размеры)
33f33df Этап 1-2: архитектура проекта, авторизация и роли
```

В рабочем дереве (ещё не закоммичено на момент записи этого файла) — Этап 6:
остатки и движения склада. Не забыть закоммитить в начале следующей сессии,
если пользователь не закоммитил сам:

- `server/src/modules/warehouses/stock/` — новый модуль (`stock.routes/
  controller/service/repository.js`), смонтирован в `app.js` на
  `/api/v1/stock` (`GET /stock/balances`, `GET /stock/movements`).
  Право — уже существовавшее `warehouse.view` (не создавалось заново).
- `client/src/features/warehouses/stock/` + `client/src/pages/warehouses/
  StockBalancesPage.jsx` / `StockMovementsPage.jsx` — читалки без CRUD,
  фильтры (склад/модель) через обычный `useState`, не через
  `EntityFormModal` (эти страницы не редактируют данные).
- Новый пункт навигации "Склады" (`AppLayout.jsx`, право `warehouse.view`) с
  подпунктами "Остатки" и "Движения" (`app/router.jsx`:
  `/warehouses/balances`, `/warehouses/movements`).
- Остатки считаются агрегацией `Instance` (`status='in_stock' AND
  archivedAt IS NULL`, GROUP BY `warehouseId/modelId/sizeId`) — **без**
  Sequelize `include` в самом агрегирующем запросе (Postgres требует все
  выбранные колонки присоединённых таблиц в GROUP BY, что хрупко); подписи
  склада/модели/размера довешиваются отдельным `findAll(where: {id: [...]})`
  и мержем в `stock.service.js`. Держаться этого паттерна, если появится ещё
  один агрегирующий отчёт (например, для Этапа 11 "Отчётность").
- Движения (`StockMovement`) — обычный `findAll` с `include`, без
  агрегации, поэтому фильтр по складу — через `Op.or` на
  `fromWarehouseId`/`toWarehouseId`.
- Backend-тесты (7/7) не трогались и не расширялись под Этап 6 — эндпоинты
  read-only, проверены вручную curl'ом и Playwright-браузером (создавал и
  затем полностью подчистил тестовые оргструктуру/склад/поставщика/размер/
  модель/документ поступления по конвенции "Тесты, создающие данные" из
  `docs/architecture.md`, хотя это не автотест, а ручная проверка).

Чек-лист по `PROJECT_SPECIFICATION.md` разделу 17 — актуальный, дублируется
в README.md "Статус реализации" (**README ещё не обновлён под Этап 6** —
сделать в начале следующей сессии вместе с коммитом):

- [x] Этап 1 — Архитектура проекта
- [x] Этап 2 — Авторизация и роли
- [x] Этап 3 — Справочники
- [x] Этап 4 — Номенклатура
- [x] Этап 5 — Закупки и оприходование
- [x] Этап 6 — Склады (остатки + история движений; инвентаризация
      сознательно не включена — она в Этапе 10, см. обсуждение ниже)
- [ ] Этап 7 — Работники ← **следующая задача**
- [ ] Этап 8 — Выдача/возврат
- [ ] Этап 9 — Ремонт/стирка
- [ ] Этап 10 — Складские документы (перемещение, инвентаризация, списание)
- [ ] Этап 11 — Отчётность
- [ ] Этап 12 — Печатные формы
- [ ] Этап 13 — Тестирование
- [ ] Этап 14 — Подготовка к эксплуатации

Рабочее дерево на момент записи этого файла: изменения Этапа 6 ещё не
закоммичены (см. выше) — проверить `git status` в начале следующей сессии.

## Локальное окружение — важные отличия от "стандартного" сетапа

- **Docker не установлен** на этой машине. `docker-compose.yml` написан и
  предназначен для сервера предприятия (раздел 4 ТЗ — развёртывание без
  интернета), но локальная разработка идёт напрямую против **локального
  PostgreSQL 18** (Windows-сервис `postgresql-x64-18`, уже запущен).
- БД `workwear_erp`, роль `workwear_app` / пароль `workwear_app_password` —
  создана отдельно от суперпользователя, не пересекается с базой `cleanflow`
  соседнего (чужого) проекта на этой же машине.
- `.env` в корне уже заполнен рабочими значениями (скопирован из
  `.env.example`), включая `BOOTSTRAP_ADMIN_PASSWORD=change-me-on-first-login`
  для входа как `admin`.
- **Git Bash на Windows ломает кириллицу в inline-аргументах** к нативным
  .exe (`curl -d '{"name":"Русский текст"}'`, `psql -c "... WHERE x = 'рус'"`)
  — байты манглятся при передаче argv в дочерний процесс. Для тестовых
  запросов с кириллицей писать payload в файл и передавать через
  `curl --data-binary @file.json` / `psql -f file.sql`, либо гонять кириллицу
  только через stdin (пайпы это не ломает). Сами данные в БД/приложении не
  повреждаются — проверено сквозным byte-level тестом, проблема только в
  этом одном инструменте тестирования.

### Запуск для проверки в этой сессии

```bash
pnpm install          # если node_modules ещё нет
pnpm db:migrate
pnpm db:seed          # создаёт роли/права/админа (admin / change-me-on-first-login)
pnpm dev              # backend :4000 (Swagger: /api-docs), frontend :5173
```

Оба dev-сервера обычно уже запущены в фоне с прошлой сессии (`node --watch`
у backend подхватывает изменения файлов сам). Проверить: `curl localhost:4000/health`
и `curl -o /dev/null -w '%{http_code}' localhost:5173`. Если не отвечают —
поднять заново командой выше.

## Как проверялась каждая фича (и как проверять дальше)

1. `pnpm --filter @workwear/server test` — автотесты (сейчас 7/7, файлы в
   `server/src/tests/*.test.js`). **Ходят в реальную dev-БД**, не в откатываемой
   транзакции — каждый новый тест обязан подчищать за собой через
   `t.after()`, см. `docs/architecture.md` раздел "Тесты, создающие данные".
2. Живой прогон в headless-браузере через Playwright, вручную установленный в
   scratchpad (`C:\Users\user\AppData\Local\Temp\claude\...\scratchpad\pw-check\`,
   там же `driver.mjs` — REPL-драйвер с командами `launch/nav/fill/click-text/
   click-dialog-text/select-label/wait-text/ss/console-errors/quit`). Это НЕ
   часть репозитория проекта — придётся либо переустановить в новом
   scratchpad (`npm install playwright && npx playwright install chromium`),
   либо написать драйвер заново по образцу `.claude` skill `run` →
   `examples/playwright.md`. Оправдало себя дважды: поймало реальные баги,
   которые не всплыли бы при проверке только по коду или только через curl
   (несработавшие defaultValue в формах, `FOR UPDATE` через LEFT JOIN).
3. После ручных проверок — чистить тестовые данные из dev-БД (см. примеры
   `DELETE FROM ...` в истории команд/`docs/architecture.md`), чтобы не
   путать реальные данные с мусором на следующей сессии.

## Следующая задача: Этап 7 — Работники

Этап 6 (Склады: остатки + движения) реализован и проверен в этой сессии —
см. запись выше. Не переоткрывать её, разве что появятся новые виды
движений от будущих документов (Выдача, Перемещение и т.д. — Этапы 8/10),
для которых уже готовый `/stock/movements` должен продолжать работать без
изменений (он читает `StockMovement` универсально по `documentType`).

Из раздела 8 ТЗ ("Работники") карточка работника должна содержать:
личные данные; стаж (расчёт автоматически); размеры; историю выдач;
историю документов; стоимость имущества; быстрый подбор комплекта
(комплекты — раздел 9 ТЗ, но подбор комплекта зависит от Выдачи, которая
ещё не реализована — Этап 8). На этом этапе разумно ограничиться CRUD
карточки работника (личные данные, организация/подразделение/должность,
дата приёма/увольнения, размеры) — расчёт стажа "на лету" по датам,
без отдельного хранимого поля. История выдач/документов и стоимость
имущества появятся содержательно только после Этапа 8 (Выдача/возврат) —
задел под них можно оставить пустым разделом карточки, не реализовывать
раньше времени.

Модуль в коде — `employees` (см. таблицу соответствий в
`docs/architecture.md`). Права уже в каталоге: `employees.view`,
`employees.manage`. Вероятно, подойдёт паттерн `CatalogPage` +
`EntityFormModal` (как справочники Этапа 3), но карточка работника
(раздел 8 ТЗ) явно богаче простого списка+формы — скорее всего понадобится
отдельная страница-карточка (в духе `ReceivingEditorPage`) для просмотра
одного работника с вкладками/секциями, а `CatalogPage`-подобный список —
только для самого списка работников. Уточнить масштаб с пользователем
перед стартом, если не очевидно после чтения раздела 8 ТЗ на месте.

## Где искать паттерны и грабли

`docs/architecture.md` — обязателен к прочтению перед новым модулем:
Controller/Service/Repository, конвенции БД (UUID, snake_case, soft-delete
через `archivedAt`), `CatalogPage`/`EntityFormModal` и их грабли
(`field.defaultValue`, `valueAsNumber`), паттерн документов
(шапка+строки+проведение, блокировка транзакции, `StockMovement`), порядок
очистки тестовых данных.

`server/src/modules/purchases/receiving/` — самый полный пример на сегодня:
routes → controller → service (транзакция проведения) → repository, плюс
zod-валидация с `preprocess` для опциональных UUID/чисел.

Не трогать `C:\Users\user\Documents\Попытка в Агбис` — отдельный личный
проект другого стейкхолдера (химчистка CleanFlow ERP), просто использовался
как ориентир по стеку в начале работы. Он вне скоупа этого проекта.
